<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>평가 문항지 - {{ title }}</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700&display=swap');
        body { font-family: 'Noto Sans KR', sans-serif; padding: 40px; line-height: 1.6; color: #333; }
        .header { text-align: center; border-bottom: 2px solid #000; margin-bottom: 30px; padding-bottom: 10px; }
        .header h1 { margin: 0; font-size: 24px; }
        .info-bar { display: flex; justify-content: flex-end; gap: 20px; margin-bottom: 20px; font-weight: bold; }
        .quiz-item { margin-bottom: 40px; page-break-inside: avoid; }
        .question { font-size: 18px; font-weight: bold; margin-bottom: 15px; }
        .choices { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
        .choice { border: 1px solid #ddd; padding: 10px; border-radius: 5px; }
        .answer-key { margin-top: 50px; border-top: 1px dashed #ccc; pt: 20px; }
        @media print {
            .no-print { display: none; }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>과학과 형성평가 ({{ subtitle }})</h1>
    </div>
    <div class="info-bar">
        <span>제 3학년 ___반 ___번  성명: ___________</span>
    </div>

    {% for q in quizzes %}
    <div class="quiz-item">
        <div class="question">{{ loop.index }}. {{ q.question }}</div>
        <div class="choices">
            {% for choice in q.choices %}
            <div class="choice">({{ loop.index }}) {{ choice.text }}</div>
            {% endfor %}
        </div>
    </div>
    {% endfor %}

    <div class="answer-key">
        <h3>[ 정답 및 해설 ]</h3>
        {% for q in quizzes %}
        <div>{{ loop.index }}번 정답: {{ q.answer }}</div>
        {% endfor %}
    </div>

    <div class="no-print" style="margin-top: 50px; text-align: center;">
        <button onclick="window.print()" style="padding: 15px 30px; font-size: 18px; cursor: pointer; background: #007bff; color: white; border: none; border-radius: 5px;">
            🖨️ 시험지 PDF로 저장/출력하기
        </button>
    </div>
</body>
</html>
